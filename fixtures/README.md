# Fixtures

- `private/` — **gitignored.** Real exported camera footage (driveway loop,
  living room). Never committed.
- `sample/` — committed. Sanitized or synthetic material a reviewer can run the
  pipeline against, plus canned `DetectionEvent` streams (JSONL) for
  ReplaySource, so the engine demo needs no camera, GPU, or network.
